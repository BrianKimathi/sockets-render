# Sockets service (Render) env example — copy to .env on Render or set in dashboard

# Render sets PORT automatically; include for local dev
PORT=10000

# CORS origins
ALLOWED_ORIGINS=http://localhost:5173,https://community-gametribe.web.app,https://gametibe2025.web.app,https://gametribe.com,https://hub.gametribe.com

# Internal emit security (must match backend SOCKETS_INTERNAL_SECRET)
INTERNAL_EMIT_SECRET=gametribe-internal-2025

# Optional Firebase Admin for verifying client tokens (if omitted, verification is skipped)
FIREBASE_PROJECT_ID=gametibe2025
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@gametibe2025.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n
FIREBASE_DATABASE_URL=https://gametibe2025-default-rtdb.firebaseio.com


